doctrine:
    dbal:
        dbname:               todocotesttest
        host:                 127.0.0.1
        port:                 8889
        user:                 root
        password:
        driver:               pdo_mysql
        unix_socket:          /tmp/mysql.sock

